/*
 *  author: Suhas Vittal
 *  datre:  11 February 2025
 * */

#ifndef DRAM_SCHEDULER_ENTRY_h
#define DRAM_SCHEDULER_ENTRY_h

#include "transaction.h"

#include <cstdint>
#include <vector>

////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////

extern uint64_t GL_DRAM_CYCLE;

////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////

struct RWQueueEntry
{
    Transaction trans;
    bool is_row_buffer_hit;

    uint64_t cycle_entered_queue;

    RWQueueEntry(Transaction t)
        :trans(t),
        cycle_entered_queue(GL_DRAM_CYCLE)
    {}
};

using dram_rw_queue_type = std::vector<RWQueueEntry>;

////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////

#endif  // DRAM_SCHEDULER_ENTRY_h
